import { StyleSheet, Text, View, SafeAreaView, StatusBar } from "react-native";
import RootNavigtion from "./navigation";

export default function App() {
  return <RootNavigtion />;
}

const styles = StyleSheet.create({});
