export const API = "https://green-states-help.loca.lt/api";

//`${deviceLT}/api`;

//"https://silly-loops-juggle.loca.lt/api";

/* const localtunnel = require("localtunnel");

const deviceLT = (async () => {
  const tunnel = await localtunnel({ port: 8000 });

  // the assigned public url for your tunnel
  // i.e. https://abcdefgjhij.localtunnel.me
  tunnel.url;

  tunnel.on("close", () => {
    // tunnels are closed
  });
})(); */
// lt --port 8000
//"https://sweet-eels-act.loca.lt/api"
//("http://10.0.2.2/api");
